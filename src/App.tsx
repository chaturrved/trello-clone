import { AppContainer } from "./styles";
import { Column } from "./components/Column";

export const App=() => {
  return (
    <AppContainer>
      <Column text="Todo:"></Column>
    </AppContainer>
  );
}